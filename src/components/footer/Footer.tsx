import React, { memo } from 'react';
import './Footer.scss';

const Footer = () => {
    return (
        <div className="footer">
            <a href="#">Back to top</a>
        </div>
    );
};

export default memo(Footer);
